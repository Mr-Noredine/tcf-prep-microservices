PORT=5002
NODE_ENV=development

# Database (en local)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tcf_prep_db
DB_USER=postgres
DB_PASSWORD=your_password

# JWT (même secret que auth-service pour vérifier les tokens)
JWT_SECRET=your_jwt_secret_key_here

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
