apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
data:
  # Exemple seulement. Remplacez par votre mot de passe encodé en base64.
  # echo -n 'votre_mot_de_passe' | base64
  POSTGRES_PASSWORD: dm90cmVfbW90X2RlX3Bhc3Nl
  # Exemple seulement. Remplacez par votre secret JWT encodé en base64.
  # echo -n 'votre_secret_jwt' | base64
  JWT_SECRET: dm90cmVfc2VjcmV0X2p3dA==
